@page "{id}"
@model CodeAid.UI.Pages.QuestionModel
@{
}

@*<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.12.0-2/css/all.min.css" integrity="sha256-46r060N2LrChLLb5zowXQ72/iKKNiw/lAmygmHExk/o=" crossorigin="anonymous" />*@
<div class="container">
	<div class="main-body p-0">
		<div class="inner-wrapper">
			<!-- Inner sidebar -->
			<div class="inner-sidebar">
				<!-- Inner sidebar header -->
				<div class="inner-sidebar-header justify-content-center">
					<button class="btn btn-primary has-icon btn-block" type="button" data-toggle="modal" data-target="#threadModal">
						<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-plus mr-2">
							<line x1="12" y1="5" x2="12" y2="19"></line>
							<line x1="5" y1="12" x2="19" y2="12"></line>
						</svg>
						NEW COMMENT
					</button>
				</div>

				<!-- Inner main body -->
				<!-- Forum List -->
				@*@foreach(var question in @Model.AllQuestions){*@


			@*	<div class="inner-main-body p-2 p-sm-3 collapse forum-content show">
					<div class="card mb-2">
						<div class="card-body p-2 p-sm-3">
							<div class="media forum-item">
								<a href="#" data-toggle="collapse" data-target=".forum-content"><img src="https://bootdey.com/img/Content/avatar/avatar1.png" class="mr-3 rounded-circle" width="50" alt="User" /></a>
								<div class="media-body">
									<h6><a href="#" data-toggle="collapse" data-target=".forum-content" class="text-body">@Model.Question.User</a></h6>
									<h3>
										@Model.Question.QuestionTitle
									</h3>
									<p class="text-secondary">
										@Model.Question.Question
									</p>
								</div>
								<div class="text-muted small text-center align-self-center">
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>*@
			<form method="post">
				<input asp-for="@Model.Question.Id" hidden />
				<input asp-for="@Model.Message.Message" />
				<button type="submit">Post</button>
			</form>

				@foreach (var qm in Model.Question.Messages)
				{
					@*@if (qm.User.Username == @Model.CurrentUser.UserName)
					{
						<div class="card-body" style="color: blue">
							<div class="alert alert-primary" role="alert">
								<p>@qm.Message---- @qm.PostDate--</p>
								<form method="post" asp-page-handler="delete">
									<button class="btn" type="submit" asp-route-id="@qm.Id">Delete</button>
								</form>
							</div>
						</div>
					}
					else
					{
						<div class="card-body">
							<div class="alert alert-primary" role="alert">
								<p>@qm.Message---- @qm.PostDate--</p>
								<form method="post" asp-page-handler="delete">
									<button class="btn" type="submit" asp-route-id="@qm.Id">Delete</button>
								</form>
							</div>
						</div>
					}*@

					<div class="card-body" style="color: blue">
							<div class="alert alert-primary" role="alert">
								<p>@qm.Message---- @qm.PostDate--</p>
								<form method="post" asp-page-handler="delete">
									<button class="btn" type="submit" asp-route-id="@qm.Id">Delete</button>
								</form>
							</div>
						</div>
				}
				
		</div>
	</div>
</div>



